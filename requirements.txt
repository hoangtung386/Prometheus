# Runtime dependencies for PrometheusNet training and inference on PUMA.
# For Colab installs only; pyproject.toml is the source of truth for version bounds.

# Keep Colab's preinstalled NumPy when it satisfies the minimum: downgrading NumPy inside a
# live kernel leaves compiled extensions ABI-mismatched.
numpy>=1.24.0
torch>=2.0.0
timm>=0.9.0
opencv-python-headless>=4.8.0
tifffile>=2023.0.0
matplotlib>=3.7                           # the `viz` extra, for notebook previews
tomli>=2.0.0; python_version < "3.11"

# Install this package itself, editable so edits take effect without reinstalling. All
# runtime dependencies are pinned above, so pip skips pyproject resolution.
-e .
